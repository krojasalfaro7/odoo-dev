<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vamos.list">
      <field name="name">vamos list</field>
      <field name="model">vamos.vamos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vamos.action_window">
      <field name="name">vamos window</field>
      <field name="res_model">vamos.vamos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vamos.action_server">
      <field name="name">vamos server</field>
      <field name="model_id" ref="model_vamos_vamos"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vamos" id="vamos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vamos.menu_1" parent="vamos.menu_root"/>
    <menuitem name="Menu 2" id="vamos.menu_2" parent="vamos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vamos.menu_1_list" parent="vamos.menu_1"
              action="vamos.action_window"/>
    <menuitem name="Server to list" id="vamos" parent="vamos.menu_2"
              action="vamos.action_server"/>
    -->
  </data>
</odoo>